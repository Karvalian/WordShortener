#include <bits/stdc++.h>

using namespace std;

int main(){
	int t;
	cout << "Enter how many times you want this program to run (How many words you want to enter) : ";
	cin >> t;
	while(t--){
		string b;
		cout << "Enter the word : " ;
		cin >> b;
		int g = b.length()-1;
		cout << b[0] << b.length()-2 << b[g] << endl;
		
	}
}
